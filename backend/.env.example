# Django
SECRET_KEY=change-me
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
FRONTEND_BASE_URL=http://localhost:5173

# Optional production origin allowlists (comma-separated). If unset, we default to FRONTEND_BASE_URL.
# CORS_ALLOWED_ORIGINS=https://yourdomain.com
# CSRF_TRUSTED_ORIGINS=https://yourdomain.com

# Realtime / Channels (recommended in production)
# REDIS_URL=redis://127.0.0.1:6379/0

# SQLite path override (used in Docker deployment)
# SQLITE_PATH=/data/db.sqlite3

# MongoDB
MONGODB_URI=mongodb://localhost:27017/splitwise
MONGODB_DB_NAME=splitwise

# Google OAuth
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Email (OTP delivery)
# Use the console backend for local development if you don't want to send real email
# EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_USE_SSL=False
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=app-password
DEFAULT_FROM_EMAIL="Splitwise Clone <your-email@gmail.com>"
SIGNUP_OTP_EXPIRATION_MINUTES=10
BYPASS_SIGNUP_OTP_CODE=000000

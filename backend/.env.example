# Django
SECRET_KEY=change-me
DEBUG=True

# MongoDB
MONGODB_URI=mongodb://localhost:27017/splitwise
MONGODB_DB_NAME=splitwise

# Google OAuth
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Email (OTP delivery)
# Use the console backend for local development if you don't want to send real email
# EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_USE_SSL=False
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=app-password
DEFAULT_FROM_EMAIL="Splitwise Clone <your-email@gmail.com>"
SIGNUP_OTP_EXPIRATION_MINUTES=10
BYPASS_SIGNUP_OTP_CODE=000000

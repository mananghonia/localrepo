# Domain names for your site. You can include multiple, comma-separated.
# Example: DOMAIN=example.com, www.example.com
DOMAIN=

# Optional: email used for Let's Encrypt registration.
# If omitted, Caddy will still work but may use an internal default.
ACME_EMAIL=
